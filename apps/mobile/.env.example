# Local Android emulator value used by scripts/verify-mobile-release.sh.
# Production builds must replace this with the deployed API origin.
EXPO_PUBLIC_API_BASE=http://10.0.2.2:8090
# Set only for local fixture acceptance builds; omit in production.
EXPO_PUBLIC_FIXTURE=1
