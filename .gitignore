*.iml
.gradle
.kotlin
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
.env
debug.keystore
*.jks
*.keystore
keystore.properties

# local tool scratch
.commandcode/

# per-module build output (root /build only covers the root project)
**/build

# Design-canvas working files. The canvas itself is a published artifact and
# these can be recovered from it; the seeded page is a 2 MB build output.
.design-brief/
